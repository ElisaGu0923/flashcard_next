import { makeStyles } from 'tss-react/mui';

const useStyles = makeStyles()((theme) => ({
    container: {
        padding: '20px 80px',
        backgroundColor: '#D4CBC3'
    }
}));

export default useStyles;